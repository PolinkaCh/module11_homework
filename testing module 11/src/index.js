/*Задание: Напишите функцию countDown(n), которая получает число {n} в качестве аргумента
 и печатает в консоль обратный отсчет, начиная с этого числаю Например, countDown(3) напечатает 3,2,1 */
export function getPercents(percent, number) {
    return number / 100 * percent  
}
  
